{
  "name": "ibuild-cleaning-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node -e \"console.log('Netlify build: static public files are ready')\""
  },
  "engines": {
    "node": ">=20"
  }
}
